# Visual Diff Notes

The following screens capture the transition from the original aer berlin UI to the brutalist refresh. Reference the `/docs/screenshots` directory for exported images (placeholders for reviewers to update with fresh captures).

1. **Home Hero** – before vs. after (`home-before.png`, `home-after.png`)
2. **Events Listing** – (`events-before.png`, `events-after.png`)
3. **Event Detail** – (`event-detail-before.png`, `event-detail-after.png`)
4. **Tickets Shop** – (`tickets-before.png`, `tickets-after.png`)
5. **Artist Detail** – (`artist-before.png`, `artist-after.png`)
6. **Gallery Grid** – (`gallery-before.png`, `gallery-after.png`)

Each “after” screenshot demonstrates:
- Updated typography hierarchy (Syne + Inter)
- Dark charcoal surfaces with magenta accent
- Poster-style cards and CTA treatments
- Motion overlays for hover/focus states (visible in video capture if available)

> Capture instructions: run `npm run dev`, visit the route, and export 1440×900 PNGs.
